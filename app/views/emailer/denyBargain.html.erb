<!DOCTYPE html>
<html>
  <head>
    <meta content="text/html; charset=UTF-8" http-equiv="Content-Type" />
	<style type="text/css">
	body {
		font-family: monospace;
		font-weight: bold;
		font-size: 13px;
		color: #333333;
		line-height: 18px;
	}

	a {
	  color: #000;
	  &:visited {
		color: #666; }
	  &:hover {
		color: #333333; } }
	</style>
  </head>
  <body>
    <h2><%= @sendingUser.name %> hat Ihren Tausch abgelehnt!</h2>
	<p>Sehr geehrte(r) <%= @receivingUser.name %>,</p>
	<p><a href="<%= url_for :action => :show, :controller => :users, :id => @sendingUser.id %>"><%= @sendingUser.name %></a> hat Ihren Tausch abgelehnt:</p>
	<p>
		Sein Produkt "<%= @sendingUserProduct.name %>"
	</p>
	<p>gegen</p>
	<p>
		Ihr Produkt "<%= @receivingUserProduct.name %>"
	</p>
	<p>Unter <a href="http://www.moerse.tk">moerse.tk</a> können Sie sich ihre Tausche ansehen und evtl. neue Tauschvorschläge unterbreiten.</p>
	<p>Herzlichst<br/>
	Ihre moerse</p>
  </body>
</html>